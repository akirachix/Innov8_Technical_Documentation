# Models

The project material references a lightweight wildlife-detection approach such as YOLOv8-nano for Raspberry Pi-class edge hardware.

**TODO / VERIFY:** record only the model actually deployed.

| Model | Purpose | Version | Input | Output | Metrics |
|---|---|---|---|---|---|
| `<model>` | Wildlife detection | `<version>` | Image | Detections | `<metrics>` |
| `<model>` | Lion identification | `<version>` | Image | Identity/unknown | `<metrics>` |
| `<model>` | Health assessment | `<version>` | Image set | Health indicators | `<metrics>` |
| `<model>` | Risk prediction | `<version>` | Event data | Risk score | `<metrics>` |